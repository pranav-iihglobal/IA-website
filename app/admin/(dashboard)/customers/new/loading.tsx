import { FormPageSkeleton } from "@/components/admin/ui";

/** The step rail and a section card — the shape the form arrives in. */
export default function Loading() {
  return <FormPageSkeleton />;
}
